# cloud-run-adk-agent
🚀 Cloud Run ADK Agent  A lightweight FastAPI application that simulates an ADK agent, deployed on Google Cloud Run using a serverless architecture. It exposes simple API endpoints for learning backend development, cloud deployment, and scalable services—optimized to run efficiently within the free tier.
